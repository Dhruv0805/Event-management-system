const Footer = () => (
  <footer className="mt-xl border-t border-border py-lg text-center text-label-md text-text-muted">
    <p>© {new Date().getFullYear()} EventHub — Event Management System. Built with the MERN stack.</p>
  </footer>
);

export default Footer;
